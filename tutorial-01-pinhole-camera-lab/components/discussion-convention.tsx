import { DISCUSSION_CONVENTION } from "@/lib/discussion-convention";

export function DiscussionConvention() {
  return <p className="discussion-convention"><strong>Coordinate convention</strong>{DISCUSSION_CONVENTION}</p>;
}
